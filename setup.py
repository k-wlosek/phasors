from setuptools import setup, find_packages

setup(
    name='phasors',
    version='1',
    packages=find_packages(),
    install_requires=[
        'matplotlib',
        'numpy',
    ],
    url='#',
    author='kw'
)